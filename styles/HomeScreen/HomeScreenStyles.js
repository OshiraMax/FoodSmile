import { StyleSheet } from 'react-native';

export const HomeScreenStyles = (themeColors) => StyleSheet.create({
    dateAndCalories: {
        marginTop: 20,
        width: '100%',
        height: '50%',
        justifyContent: 'center',
        alignItems: 'center',
    },
});